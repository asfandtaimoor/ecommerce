const path = require('path');

module.exports = {
  config: path.resolve('./config/config.json'), // Path to your config file
  'models-path': path.resolve('./models'), // Path to models
  'migrations-path': path.resolve('./migrations'), // Path to migrations
  'seeders-path': path.resolve('./seeders'), // Path to seeders
};
